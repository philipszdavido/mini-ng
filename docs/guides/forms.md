# Guides — Forms

Forms support is minimal; prefer template-driven approaches or simple host bindings. See `core/framework/forms/forms.ts` for primitives.

Example

```html
<input [(ngModel)]="user.name" />
```
